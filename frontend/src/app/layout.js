import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {/* ...existing code or additional layout components... */}
        {children}
      </body>
    </html>
  );
}
